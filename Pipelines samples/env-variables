pipeline {
    agent any

    environment {
        NAME = 'John'
        FAMILYNAME = 'Doe'
    }

    stages {
        stage('Build') {
            steps {
                sh 'echo $NOMBRE $APELLIDO'
            }
        }
    }
}